<!DOCTYPE html>
<html>
<head>
    <title>Breathing Exercise</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<style>
    body {
             background-color: #f8c990;
             color: rgb(4, 33, 53);
         }
 </style>
<body>
    <div class="container text-center mt-5">
        <h1 class="mb-4">Breathing Exercise</h1>
        <h3>Follow the breathing instructions to relax</h3>
        <p> Pause now if you feel tired and come back to it</p>
        <div id="breathingExercise" class="mt-4">
            <button class="btn btn-primary btn-lg mb-3" onclick="startBreathing()">Start Breathing Exercise</button>
            <div id="instructions" class="mt-3 font-weight-bold" style="font-size: 24px;"></div>
        </div>
         <!-- Progress Bar -->
     <div class="progress mt-5">
        <div id="progress-bar" class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"
            data-gameid="<%= gameId %>" data-userid="<%= userName %>"></div>
    </div>
    <p id="time-remaining" class="mt-3">Time remaining: 15 minutes 0 seconds</p>
    <button class="btn btn-secondary mt-3" onclick="window.location.href='/games'">Back to Games</button>
    </div>
    
</div>
    <script src="/progressTracker/progress.js"></script>
    <script>
        let steps = [
            'Breathe in for 4 seconds',
            'Hold for 4 seconds',
            'Breathe out for 4 seconds',
            'Hold for 4 seconds'
        ];
        let stepIndex = 0;
        function startBreathing() {
            document.getElementById('instructions').innerText = steps[stepIndex];
            setInterval(() => {
                stepIndex = (stepIndex + 1) % steps.length;
                document.getElementById('instructions').innerText = steps[stepIndex];
            }, 4000);
        }
    </script>

   </body>
</html>
